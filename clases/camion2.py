class Camion:
    def __init__(self, posX, posY):
        self.posicion = [posX, posY]  # No varía una vez asignado   
        self.camionLista = []

    valorDevuelto = self.pintarPaquetesCamion()

    def pintarPaquetesCamion(self):

        if paquete in matrizPaquetes(0, 0) and luigi in (len(posicion y - 1)): #me parece que las filas empiezan en 0 y bajan (0, 1 ,2...)                                         #No tengo muy clara la posicion de luigi pero se entiende la idea jeje
            pop paquete(0, 0)
            self.camionLista.append(1) #creo que se dibuja solo con lo de que se dibuja un paquete en los 1 que implementaste
        else:
            pop paquete(0, 0)
        # pintar un rectangulo donde en la lista camionLista haya un 1

while len(camionLista) == 8:
    stop paquetes de la matriz e igual a los personajes #tengo que ver como se implementa
    posX -= pyxel.frame_count % 30
    if posX == tal: #cuando salga de la pantalla
        del camionLista(all)
        while posX != x inicial: 
            posX += pyxel.frame_count % 30
    
